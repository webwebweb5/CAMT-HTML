let code = {
    code: 10,
    status: "complete"
};

var id = prompt("The code is 6+4 :");
switch(parseInt(id)){
    case 10:
        alert(code);
        break;
    default:
        alert("Invalid code")
}

